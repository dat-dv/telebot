version: '3.8'

services:
  telebot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram-assistant-bot
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
